Model files contain space-delimited key-value lines which represent a model.
The easiest model contains just a model and diffuse texture.
More sophistopcated model are possible with the use of the appropriate
key-value associations as defined in here and `MATERIALS'

The following keys exist:

* model
    The model file
